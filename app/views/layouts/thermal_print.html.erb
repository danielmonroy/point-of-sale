<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Akymbo Café Narvarte</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>
  <body class="sidebar-fixed" style="width:500px">
    <div class="container-scroller">
      <div class="container-fluid page-body-wrapper">
        <%= yield %>
      </div>
      <!-- page-body-wrapper ends -->
    </div>
  </body>
</html>

